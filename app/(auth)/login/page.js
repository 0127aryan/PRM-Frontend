import { LoginForm } from '@/components/auth/login-form';

export const metadata = {
  title: 'Sign in — PRM Tool',
  description: 'Sign in to Project & Resource Management',
};

export default function LoginPage() {
  return <LoginForm />;
}
